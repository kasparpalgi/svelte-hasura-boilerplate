See how I have connected Svelte to Hasura and I have codegen in another project outside of this project's root folder at ../svelte-todo-kanban and implement here the same way. 

1. Learn how there is done. In /CLAUDE.md it is also already described how the backend shall work and it is copied from that project
2. Make in thids project the same way codegen and Hasura API connection just npm install latest versions
3. In /hasura folder with Hasura CLI create a minimal table 'users' with just 'uuid', 'name', 'email', 'created_at'
4. Migrate and add a test user so you can create a test page to fetch users and see with Playwright that user when you open it in dev server

## Analysis
- Affected files:
  - `src/lib/graphql/documents.ts` — added `GET_USERS` query
  - `src/lib/graphql/generated/` — generated by codegen (gql.ts, graphql.ts, index.ts, fragment-masking.ts)
  - `src/lib/graphql/server-client.ts` — new: server-side client with admin secret
  - `src/routes/test-users/+page.server.ts` — loads users from Hasura
  - `src/routes/test-users/+page.svelte` — displays users
  - `tests/e2e/test-users.e2e.ts` — Playwright test
  - `fix-imports.cjs` — updated to create `fragment-masking.ts` when missing (newer client-preset v5.2.4 stopped generating it)
  - `hasura/migrations/default/1775168886272_create_users_table/` — SQL migration
  - `hasura/metadata/databases/default/tables/public_users.yaml` — table metadata + permissions
  - `hasura/seeds/default/1_test_users.sql` — test user seed

## Implementation Notes
- codegen, client.ts, fix-imports.cjs were already present in the boilerplate
- fix-imports.cjs extended: newer `@graphql-codegen/client-preset` v5.2.4 generates `./fragment-masking` imports directly but no longer outputs `fragment-masking.ts`; script now creates it on every codegen run
- Bootstrap pattern: first codegen run uses `/* GraphQL */` comment, then switch to typed `graphql()` tag
- Test page uses server-side load (`+page.server.ts`) with admin secret — avoids needing JWT auth for the demo

## Verification
- [x] Browser tested (Playwright e2e: 1 passed)
- [x] DB verified (migration applied, seed planted via `hasura seed apply`)
- [x] `npm run check` passed (0 errors)
- [x] Playwright test passed
