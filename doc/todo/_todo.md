* https://github.com/DietrichGebert/ponytail
* git pull before doing anything (webhook)
* git pull, commit, push (webhook? commit must be with message so the AI must write that into somewhere hook can take if makes sense to webhook this at all)